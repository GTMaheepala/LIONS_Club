# Optional locally: CRA defaults to http://localhost:5000 when unset (npm start).
# Required for production build (e.g. Vercel): your deployed API origin, no trailing slash.
REACT_APP_API_URL=http://localhost:5000
